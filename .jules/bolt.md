# Bolt's Journal

## 2024-05-22 - Missing Media Tools
**Learning:** The environment lacks `ffmpeg` and `imagemagick`, preventing direct asset optimization (resizing images, compressing video).
**Action:** Focus on HTML attribute optimizations (preload, lazy loading) rather than asset modification.
