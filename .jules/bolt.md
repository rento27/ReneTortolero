# Bolt's Journal

## 2024-05-23 - Static Site Optimization
**Learning:** Even simple static sites can have significant performance bottlenecks like 404s on assets and missing resource hints.
**Action:** Always check console for 404s on media assets and audit `<video>`/`<img>` tags for `preload` and `loading` attributes.
