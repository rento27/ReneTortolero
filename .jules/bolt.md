## 2024-05-23 - Critical Learning: Missing Media Handling Tools
**Learning:** The environment lacks tools like `ffmpeg` or `imagemagick`, preventing direct resizing or frame extraction of assets.
**Action:** When optimizing media, focus on attribute-based optimizations (like `preload`, `loading="lazy"`) or removing dead links, rather than trying to resize or convert files.
