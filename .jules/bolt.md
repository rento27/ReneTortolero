# Bolt's Journal - Critical Learnings

## 2024-05-22 - Initial Setup
**Learning:** The environment lacks media manipulation tools like `ffmpeg` or `imagemagick`.
**Action:** Focus on HTML attribute optimizations (preload, lazy loading) rather than asset processing.
